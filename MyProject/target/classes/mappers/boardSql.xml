<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<insert id="writeboard" parameterType="kr.co.korea.beans.BoardBean">
	<selectKey keyProperty="bid" order="BEFORE" resultType="Integer">
		select board_seq.nextval from dual
	</selectKey>
		 insert into board(bid,btitle,bcontent,bwriter)
		 values(#{bid},#{btitle},#{bcontent},#{bwriter})
	</insert>
	
	<select id="readboard" parameterType="Integer" resultType="kr.co.korea.beans.BoardBean">
		select bid,btitle,bcontent,to_char(bdate,'YYYY-MM-DD') as bdate ,bwriter 
		from board	
		where bid=#{bid}	
	</select>
	
	<select id="getboardlist" resultType="kr.co.korea.beans.BoardBean">
		select bid,btitle,bcontent,to_char(bdate,'YYYY-MM-DD') as bdate ,bwriter 
		from board
	</select>
</mapper>